# Ambiente
.venv/
__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/
.coverage
htmlcov/

# Segredos locais
.env

# Artefatos de modelo (gerados por `make train` / CI, não versionados)
models/*.joblib
models/*.onnx
models/*.pt

# Dados processados (derivados do raw, regeneráveis)
data/processed/*
!data/processed/.gitkeep

# reports/latency_comparison.json é commitado (entregável da Etapa 4);
# outros relatórios gerados localmente ficam de fora por padrão.
reports/*.json
!reports/.gitkeep
!reports/latency_comparison.json

# MLflow / logs locais (se usados em experimentação futura)
mlruns/
*.log

# SO / editor
.DS_Store
.vscode/
